/**
 * Shared Types
 * @layer Domain (Contracts)
 *
 * Re-exports type contracts from shared layer.
 * Import from "@octopus-synapse/profile-ui/shared" for full access.
 */

// Re-export all types from shared layer
export * from "../shared";
